# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際に**必ず**参照する恒久的な開発ルールです。
個々のタスク指示より優先度は下ではありませんが、タスク指示がこのファイルと矛盾する場合は、まずユーザーに確認してください（勝手にどちらかを優先しない）。

## 1. リポジトリの現状（2026年時点、変更のたびに更新すること）

- **ビルドツールなしの静的サイト**。`package.json` は存在しない。npm/yarn のインストール手順は不要。
- 主なファイル：
  - `index.html` — 既存公開ページ（「3分でできる 購入合格ラインチェック」）
  - `stress-test.html` — 開発中のツール（v13〜v18と反復改善中。現在の中心概念は「住宅・現金・金融資産への配分整理」）
  - `vercel.json` — Vercel設定（`cleanUrls: true` により `foo.html` は `/foo` でも配信される）
  - `README.md` — 簡易メモ（正式なドキュメントではない）
- **自動テストフレームワークは存在しない。** lint設定・Playwright設定もリポジトリには含まれていない。検証は毎回、環境にあるツール（`node`, `npx eslint`, `npx http-server`, `npx playwright` あるいはグローバルにインストール済みのもの）をその場で使う。下記「4. 実装後に必ず行う確認」を参照。
- デプロイ：GitHubへのpush→Vercelが自動的にプレビューデプロイを作成する（GitHub連携済み）。

このセクションが古くなっていると気づいたら、作業のついでに更新してよい（勝手にリポジトリ構成を変える、という意味ではなく、事実の記述を直す）。

## 2. プロダクトの目的（最重要・絶対に見失わないこと）

このツールは**住宅購入可能額を単純に引き上げるための計算機ではありません**。実装判断に迷ったら、必ずこの目的に立ち返ること。

1. 住宅に使える予算には複数の選択肢があり、そこに生まれる「余白」を可視化することが目的である。
2. 「買える金額」と「安心して使える金額」は別物であり、混同する表現・設計をしない。
3. 年収倍率だけで住宅予算を決めさせない。年収倍率は**内部計算の結果を分かりやすく示す参考値**であり、価格算出の入力に固定倍率として使わない（`stress-test.html` の `PAYMENT_RATIO_BASE` → `priceFromPaymentRatio()` の実装を参照。倍率は月々の返済配分から逆算した**結果**として表示する）。
4. 家族構成（特に子どもの人数・年齢帯）による違いを無視しない。既存の個別補正ロジック（`adjustments()`）は、この目的のために存在する。
5. 不安・迷いがある場合は、教育費・生活費・レジャー費・老後資金・資産運用まで含む詳細なライフプラン確認へ自然につなげる（CTA・補足文の役割）。
6. 営業側に都合の良い結論（「もっと高い物件を買うべき」「この金額まで安全」等）へ誘導しない。断定表現を避け、比較のための目安であることを明記する。
7. ユーザー自身が選択肢を理解し、自分で判断できるように設計する。ツールが代わりに結論を出さない。

新しい文言・機能を追加するときは、上記7点のいずれかに違反していないか必ず自問すること。特に「〜倍まで安全」「〜を買うべき」のような断定・誘導表現は禁止。

## 3. 開発ルール（絶対厳守）

1. **mainブランチへ直接変更しない。** 作業用ブランチを作成し、そこにcommit・pushする。ユーザーから明示の指示がない限り、mainへのマージ・本番デプロイは行わない。
2. **作業前に関連コードと影響範囲を確認する。** 該当ファイルを実際に読んでから編集する。特に `stress-test.html` は計算ロジックと表示ロジックが密結合しているため、変更対象の関数がどこから呼ばれているか（`grep`等で）確認してから着手する。
3. **指示されていない仕様変更を勝手に行わない。** 「ついでの改善」「気づいた不具合の修正」であっても、指示範囲外なら先にユーザーに確認するか、少なくとも報告時に明示する。
4. **不明な仕様は推測で決めない。** 数値の前提（金利ストレスの水準、諸費用率、しきい値など）が指示に無く重要な意味を持つ場合は、ユーザーに確認するか、選定理由を明記した上で保守的な値を選び、必ず完了報告で明示する。
5. **必要以上のリファクタリングをしない。** 動いている既存コードのスタイル変更・整理は、指示されない限り行わない。修正は最小差分を心がける。
6. **既存の正常な機能を壊さない。** 変更前に「今回のタスクと無関係の機能」が何かを把握し、実装後に回帰確認する（4節参照）。
7. **UI変更時はPC・モバイル双方を意識する。** このプロジェクトはiPhone 390px幅を最優先ターゲットとしている（横スクロール禁止、縦カード中心、固定ナビが本文を隠さないこと）。あわせてデスクトップ幅（例：1280px）でも崩れないことを確認する。
8. **計算ロジックを変更する場合は、変更前後の数値を必ず検証する。** 少なくとも：
   - 変更前・変更後で同一条件の計算結果を比較する。
   - 可能な場合は、実装と異なるアプローチ（総当たりシミュレーション、別実装での再計算など）で独立に検算する。
   - 借入期間・金利など複数の入力を動的に変える場合は、代表的な組み合わせ（例：期間30/35/37/40/42/45年、金利1.0/1.3/1.5/2.0%）で単調性・連動性を確認する。
   - 固定テーブル・固定倍率による近似に戻していないか確認する（本プロジェクトでは過去に「年収×固定倍率」方式が問題視され、動的な逆算方式に修正した経緯がある）。

## 4. 実装後に必ず行う確認

自動テストフレームワークがないため、以下を**その都度手動で組み立てて実行する**。手順は使い捨てのスクリプトで構わないが、実行はスキップしない。

### 4.1 lint（構文チェック含む）
HTMLに埋め込まれた `<script>` を取り出し、`node --check` と `eslint` にかける。
```bash
node -e "
const fs=require('fs');
const html=fs.readFileSync('stress-test.html','utf8');
const m=html.match(/<script>([\s\S]*)<\/script>/);
fs.writeFileSync('/tmp/extracted.js', m[1]);
"
node --check /tmp/extracted.js
npx eslint /tmp/extracted.js   # ローカルconfigが無いため、必要なら flat config を一時生成して使う
```
`index.html` を変更した場合も同様に行う。

### 4.2 既存テスト
現時点でリポジトリに自動テストは存在しない。「既存テストの実行」に該当する作業は無い。ただし、計算ロジックに触れた場合は 3-8 に従って検算スクリプトを書き、その場で実行すること（このスクリプトはリポジトリにコミットしなくてよいが、完了報告に検証内容・結果を明記する）。

### 4.3 追加テスト（関連機能）
今回の変更が影響しうる入力の組み合わせを洗い出し、Playwrightで実データを流して確認する（下記4.4と統合してよい）。年齢・子どもの人数・借入方法・金利タイプ・借入期間など、分岐やしきい値がある項目は必ず境界値を含める。

### 4.4 Playwrightによる主要画面確認
このサンドボックスにはChromiumが用意されている（`PLAYWRIGHT_BROWSERS_PATH` を確認。無ければ `npx playwright install chromium` を先に実行）。ローカルに軽量サーバーを立ててから操作する。
```bash
npx http-server -p 8734 -s .   # バックグラウンドで起動
```
```js
// Node script 例
const { chromium } = require('playwright'); // またはグローバルインストール先を直接 require
const browser = await chromium.launch();
for (const viewport of [{width:390,height:844}, {width:1280,height:900}]) {
  const page = await (await browser.newContext({ viewport })).newPage();
  page.on('pageerror', e => console.log('pageerror', e.message));
  page.on('console', m => { if (m.type() === 'error') console.log('console.error', m.text()); });
  await page.goto('http://127.0.0.1:8734/<対象ページ>');
  // 主要画面（トップ→入力→結果）を実際に操作して遷移させる
}
```
最低限、トップ画面→入力画面→結果画面の一連の遷移を、iPhone幅（390px）とデスクトップ幅（1280px）の両方で確認する。入力画面の分岐（子どもの人数、金利タイプ、借入方法など）も変えて結果画面まで到達させる。

### 4.5 console error確認
4.4のPlaywrightスクリプトで `pageerror` と `console.error` を収集し、**0件であること**を確認する。1件でもあれば原因を特定して修正し、再テストする。

### 4.6 問題が見つかった場合
原因を特定して修正し、4.1〜4.5を再実行する。テストを緩めたり、確認項目を削って「通った」ことにしない。

## 5. 完了報告のフォーマット

作業完了時は必ず以下を報告する。

- 変更内容（要約）
- 変更したファイル（一覧）
- 実行したテスト（コマンド・シナリオ）
- テスト結果（数値検証の代表結果を含む。数値ロジックを変えた場合は変更前後の比較を示す）
- 残っている問題・不明点・ユーザーに確認が必要な仕様判断
- 人間がVercel Previewで目視確認すべき点

## 6. Git運用

- 作業はブランチ上で行い、mainには直接pushしない。
- 指示がない限りmainへのマージ・本番反映は行わない。
- コミットメッセージには「何を」だけでなく「なぜ」「どう検証したか」を書く（このリポジトリの既存コミット履歴のスタイルに合わせる）。
- 差分は最小限にし、無関係なファイル・整形変更を混入させない。
