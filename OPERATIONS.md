# 運用ルール

## 正本
- GitHub `main` を唯一の正本とする。
- 通常の更新は `main` へ反映し、GitHub連携によるVercel自動デプロイを利用する。

## 公開確認
1. GitHub `main` の最新コミットSHAを確認する。
2. そのコミットの `Vercel` status が `success` になっていることを確認する。
3. `pending` / `failure` の場合は公開完了扱いにしない。

## 本番URL
- 固定のProduction URLは、実URLを取得・疎通確認できた時点でここへ記録する。
- プロジェクト名から `.vercel.app` URLを推測して記載しない。

## 変更時の最低確認
- 計算ロジックを変更した場合は `calculation-tests.js` の回帰テスト条件を維持する。
- UIのみの変更でも、入力→計算→結果表示の基本フローを壊していないか確認する。
- 購入・賃貸のどちらか一方を有利に見せる表現に変更しない。

## 基準仕様
- 2026-09-25時点の最新版仕様を基準とする。
- PDFは使わずWebツールとして運用する。
